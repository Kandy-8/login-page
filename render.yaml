services:
  - type: web
    name: login-backend
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: NODE_ENV
        value: production

